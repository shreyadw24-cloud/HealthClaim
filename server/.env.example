GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
PORT=3000
SUPABASE_URL=
# Service-role key, not the anon/public key — this runs server-side only
# and needs full insert/select access to the verifications table.
SUPABASE_SERVICE_KEY=